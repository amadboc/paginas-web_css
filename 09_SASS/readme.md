SASS Compilador (3 formas de instalar SASS)

- sass en ruby: gem -g sass (la original escrita en ruby)
- node -sass: npm i -g node-sass (se intala con npm pq es JS)
- sass en JS: npm i -g sass (lo que hemos instalado nosotros.
Si pongo en la consola sass --version me dice la versión que tengo instalada).

SASS DartJS

sass archivo.scss archivo.css (archivo entrada y arch salida)
sass archivo.scss:archivo.css (tb funciona con :)


